import { Helmet } from "react-helmet-async";

const Add = () => {
  return (
    <div>
      <Helmet>
        <title>Add | Dashboard</title>
      </Helmet>

      {/* Form */}
    </div>
  );
};

export default Add;
